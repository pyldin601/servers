apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myownradio-mysql-data-volume-claim
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
