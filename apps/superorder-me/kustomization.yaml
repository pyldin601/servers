apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: arbitrary

commonLabels:
  app: superorder-me

resources:
  - dockerconfig-sealedsecret.yaml
  - superorder-binance-adapter-deployment.yaml
