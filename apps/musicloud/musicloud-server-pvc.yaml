apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: musicloud-server-data-volume-claim
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 32Gi
