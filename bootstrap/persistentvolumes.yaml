apiVersion: v1
kind: PersistentVolume
metadata:
  name: k8s-persistent-volume
spec:
  storageClassName: manual
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/volumes/k8s"
