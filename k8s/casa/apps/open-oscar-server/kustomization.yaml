apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: arbitrary

labels:
  - pairs:
      app: open-oscar-server
    includeSelectors: true

resources:
  - namespace.yaml
  - open-oscar-server.yaml
